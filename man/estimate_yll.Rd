% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{estimate_yll}
\alias{estimate_yll}
\title{Estimate years of life lost in a specified target population}
\usage{
estimate_yll(
  data,
  id_var = "id",
  time_var,
  event_var,
  exposure_var,
  reference_level = NULL,
  exposed_level = NULL,
  age_at_entry_var,
  target_population,
  age_start = 50,
  age_end = 100,
  age_interval = 5,
  confounders_baseline = NULL,
  B = 1000,
  seed = 1,
  conf_level = 0.95,
  ci_method = c("normal", "percentile"),
  integration = c("left_rectangle", "trapezoidal"),
  show_progress = TRUE,
  use_future = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per person and no missing values in
the selected variables. Remove incomplete records explicitly before use.}

\item{id_var}{Name of the unique person identifier column.}

\item{time_var}{Name of the positive follow-up duration column, in years.}

\item{event_var}{Name of the event indicator column, coded 0/1.}

\item{exposure_var}{Name of the binary exposure column.}

\item{reference_level, exposed_level}{Observed exposure values corresponding
to x = 0 and x = 1. Specify both to make the direction explicit. If omitted,
factor order (or order of appearance for other types) is used and reported.}

\item{age_at_entry_var}{Name of the age-at-entry column, in years.}

\item{target_population}{Required. One of \code{"all"}, \code{"exposed"}, or
\code{"unexposed"}. Selects the baseline covariate distribution used for
standardization, not the data used to fit the hazard model.}

\item{age_start}{First starting age at which to report ERL and YLL.}

\item{age_end}{Upper age limiting every ERL integral. Also the upper bound
of the reporting sequence. Must be at least \code{age_start}.}

\item{age_interval}{Spacing between reported starting ages. Results are
reported at \code{seq(age_start, age_end, age_interval)}. The main estimator
requires integer ages and uses a one-year integration grid regardless
of this reporting interval.}

\item{confounders_baseline}{Character vector of baseline covariate columns.}

\item{B}{Number of participant bootstrap replicates, at least 2, or 0 to
obtain point estimates only. Failed replicates are reported in a warning
and recorded in the result metadata; intervals use successful replicates.}

\item{seed}{Integer random seed. The caller's random state is restored.}

\item{conf_level}{Confidence level, strictly between zero and one.}

\item{ci_method}{\code{"normal"} (default) uses the bootstrap standard error;
\code{"percentile"} uses bootstrap quantiles. Both resample people with replacement.}

\item{integration}{\code{"left_rectangle"} (default) or \code{"trapezoidal"}.}

\item{show_progress}{Whether to show bootstrap progress. Parallel progress
uses the caller's progressr handlers.}

\item{use_future}{Use the caller's future plan for bootstrapping. The
package never changes the plan or starts a worker pool itself.}
}
\value{
A list with five elements:
\itemize{
\item \code{summary}: one row per \code{starting_age}, with \code{erl_reference}, \code{erl_exposed},
\code{yll}, \code{yll_se}, \code{ci_low}, and \code{ci_high}, all measured in years.
\item \code{bootstrap_estimates}: the four point-estimate columns plus \code{iteration}.
\item \code{survival_curves}: \code{starting_age}, \code{age}, \code{survival_reference}, and
\code{survival_exposed}, averaged over the target population.
\item \code{bootstrap_survival_curves}: the same curve columns plus \code{iteration}.
\item \code{meta}: model, target population, exposure labels, age window, interval
method, bootstrap settings, successful replicate count, and failure details.
\code{bootstrap_unreliable} records finite bootstrap ERLs outside the possible
age window. These trigger a warning and are retained, not silently removed.
}

With \code{B = 0}, intervals and standard errors are \code{NA} and bootstrap tables
are empty. Curves are standardized after conditioning each person on
survival to the starting age. They are not curves obtained by first
averaging unconditional survival and then conditioning that average.
Intervals are pointwise, not simultaneous confidence bands.
}
\description{
Fit a pooled logistic hazard model on the full analytic sample, predict
survival under both exposure levels, and average individual conditional
survival curves over the specified baseline population. The contrast is
always restricted expected residual lifetime (ERL) under the reference
level minus ERL under the exposed level. Its sign never depends on the
target population.
}
\details{
The fitted model includes exposure, a natural cubic spline in
attained age, their interaction, and the supplied baseline covariates.
Follow-up is split into one-year intervals after cohort entry. Predictions
represent sustained exposure scenarios, not exposure changes initiated at
the starting age. Causal interpretation requires consistency, exchangeability,
positivity, appropriate censoring and entry assumptions, and correct model
specification. Predictions beyond observed age support require extrapolation.
The same baseline covariate distribution is retained at every starting age.
}
\examples{
data(yll_toy)
result <- estimate_yll(
  yll_toy[1:500, ], time_var = "period", event_var = "event",
  exposure_var = "hypertension", reference_level = "No", exposed_level = "Yes",
  age_at_entry_var = "age", target_population = "unexposed",
  age_start = 50, age_end = 70, age_interval = 10,
  confounders_baseline = "sex", B = 0, use_future = FALSE
)
result$summary
}
