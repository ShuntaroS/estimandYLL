% Generated by hand for the simplified public API.
\name{estimand_yll}
\alias{estimand_yll}
\alias{estimate_yll}
\title{Estimate years of life lost from a clearly stated estimand}
\usage{
estimand_yll(
  data,
  id_var = "id",
  time_var,
  event_var,
  exposure_var,
  reference_level = NULL,
  exposed_level = NULL,
  age_at_entry_var,
  target_population = c("all", "exposed", "unexposed"),
  intervention = c("full_contrast", "partial_change"),
  change_from = NULL,
  change_to = NULL,
  change_probability = NULL,
  measure = c("yll", "life_year_change"),
  age_start = 50,
  age_end = 100,
  age_interval = 5,
  confounders_baseline = NULL,
  B = 1000,
  seed = 1,
  conf_level = 0.95,
  method = c("normal", "percentile"),
  integration = c("left_rectangle", "trapezoidal"),
  show_progress = TRUE,
  use_future = TRUE
)
estimate_yll(...)
}
\arguments{
\item{data}{A \code{data.frame} with one row per individual.}
\item{id_var, time_var, event_var, exposure_var}{Column names in \code{data}.}
\item{reference_level, exposed_level}{Values labelling the binary exposure levels.}
\item{age_at_entry_var}{Column name for age at study entry.}
\item{target_population}{Who to average over: \code{"all"}, \code{"exposed"}, \code{"unexposed"}, or a custom function.}
\item{intervention}{\code{"full_contrast"} for all-reference versus all-exposed, or \code{"partial_change"} for a policy-like partial change from the natural exposure distribution.}
\item{change_from, change_to, change_probability}{Partial-change intervention settings. Use \code{"reference"} or \code{"exposed"} for \code{change_from} and \code{change_to}.}
\item{measure}{\code{"yll"} for \code{LE_reference - LE_exposed}; \code{"life_year_change"} for the clinically oriented life expectancy change.}
\item{age_start, age_end, age_interval}{Starting ages for which estimates are reported.}
\item{confounders_baseline}{Character vector of baseline confounder column names.}
\item{B, seed, conf_level, method, integration, show_progress, use_future}{Bootstrap and numerical-integration settings.}
\item{...}{Arguments passed from the compatibility alias \code{estimate_yll()} to \code{estimand_yll()}.}
}
\description{
Main user-facing interface for the package. The estimand is specified by three explicit components: target population, intervention, and measure.
}
\value{
A list containing \code{summary}, \code{detailed_results}, \code{meta}, and marginal survival curves.
}
